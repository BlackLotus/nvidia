# $Id: PKGBUILD 9364 2008-08-17 06:29:43Z allan $
# Maintainer: dorphell <dorphell@archlinux.org>
# Contributors: John Proctor <jproctor@prium.net>
#               Jeramy Rutley <jrutley@gmail.com>
pkgname=ruby
pkgver=1.8.7_p72
_pkgver=1.8.7-p72
pkgrel=2
pkgdesc="An object-oriented language for quick and easy programming"
arch=(i686 x86_64)
license=('custom')
url="http://www.ruby-lang.org/en/"
depends=('db>=4.7' 'openssl>=0.9.8h-3' 'zlib' 'readline' 'termcap-compat')
makedepends=('tk>=8.5')
source=(ftp://ftp.ruby-lang.org/pub/ruby/stable/${pkgname}-${_pkgver}.tar.bz2) 
options=('!emptydirs' 'force')
md5sums=('0b215c46b89b28d7ab8d56d96e72d5b9')

build() {
  cd ${startdir}/src/${pkgname}-${_pkgver}
  ./configure --prefix=/usr --enable-shared --enable-pthread --without-gdbm
  make || return 1
  make DESTDIR=${startdir}/pkg install
  #generate the ri docs
  make -j1 DESTDIR=${startdir}/pkg install-doc
  install -Dm644 COPYING ${startdir}/pkg/usr/share/licenses/${pkgname}/COPYING
}
