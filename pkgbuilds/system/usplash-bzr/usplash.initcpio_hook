# vim: set ft=sh:
run_hook ()
{
    [ -x /sbin/usplash ] || return 1

    [ ${splash} ] || return 0

    [ ${quiet} ] || varg='-v'

    [ -f /etc/usplash.conf ] && . /etc/usplash.conf

    /sbin/usplash -p -c $varg &
}
